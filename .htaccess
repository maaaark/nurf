    SetEnv HTTP_MOD_REWRITE On
    RewriteEngine On

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
        
    # Alles auf index umleiten
    RewriteRule ^(.*)                 index.php?page=$1 [QSA,L]
